name: Deploy Ozark Gazette

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  static-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: List files
        run: find . -maxdepth 3 -type f | sort
